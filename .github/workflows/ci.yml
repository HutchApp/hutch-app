name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

permissions:
  contents: read

jobs:
  all-tests:
    runs-on: ubuntu-latest
    environment: staging
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: ap-southeast-2
      PULUMI_BACKEND_URL: s3://hutch-pulumi-state-572337278115-ap-southeast-2
      PULUMI_STACK: staging
      PULUMI_CONFIG_PASSPHRASE: ''
      PERSISTENCE: development
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      STATIC_BASE_URL: 'https://d2uoxssv1ey7em.cloudfront.net'
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm check

      # Re-run the web-embed visual tests with --update-snapshots so CI-rendered
      # linux pixels land in the snapshot dir, then upload them as an artifact
      # for visual comparison against the committed darwin baselines.
      - name: Capture web-embed CI screenshots
        if: always()
        working-directory: projects/web-embed
        env:
          HEADLESS: 'true'
          E2E_PORT: '3700'
        run: |
          mkdir -p ci-screenshots
          # Delete committed linux baselines so --update-snapshots regenerates
          # them with the fresh CI-rendered pixels. Without this, Playwright
          # sees the tests pass within the 10% tolerance and skips the update,
          # leaving the uploaded artifact byte-identical to the committed file.
          rm -f src/e2e/visual.e2e-local.ts-snapshots/*-chromium-linux.png
          pnpm exec playwright test \
            --config playwright.config.local-dev.ts \
            --update-snapshots \
            --reporter=list || true
          cp src/e2e/visual.e2e-local.ts-snapshots/*-chromium-linux.png ci-screenshots/

      - name: Upload web-embed CI screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: web-embed-ci-screenshots
          path: |
            projects/web-embed/ci-screenshots/
            projects/web-embed/test-results/
          retention-days: 14
          if-no-files-found: warn

  # Only detect and deploy on pushes to main (not on PRs)
  detect-projects:
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    environment: staging
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: ap-southeast-2
      PULUMI_BACKEND_URL: s3://hutch-pulumi-state-572337278115-ap-southeast-2
      PULUMI_STACK: staging
      PULUMI_CONFIG_PASSPHRASE: ''
    # Expose detection results so downstream jobs can conditionally run
    outputs:
      platform-affected: ${{ steps.detect.outputs.platform-affected }}
      non-platform-projects: ${{ steps.detect.outputs.non-platform-projects }}
      has-non-platform: ${{ steps.detect.outputs.has-non-platform }}
      parallel-projects: ${{ steps.detect.outputs.parallel-projects }}
      has-parallel: ${{ steps.detect.outputs.has-parallel }}
      web-embed-affected: ${{ steps.detect.outputs.web-embed-affected }}
      ff-affected: ${{ steps.detect.outputs.ff-affected }}
      chrome-affected: ${{ steps.detect.outputs.chrome-affected }}
    steps:
      - uses: actions/checkout@v6
        with:
          # Full history needed for nx affected to compare commits
          fetch-depth: 0

      # Sets NX_BASE and NX_HEAD so nx affected knows what commits to compare
      - uses: nrwl/nx-set-shas@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - id: detect
        run: |
          # Get the list of projects affected by the push (uses NX_BASE..NX_HEAD)
          AFFECTED=$(pnpm nx show projects --affected --json)
          echo "Affected projects: $AFFECTED"

          # Dynamically discover all projects that have a "deploy-infra" target
          DEPLOYABLE=$(pnpm nx show projects --with-target deploy-infra --json)

          # Intersect: projects that are both affected AND deployable
          ALREADY_DEPLOY=$(echo "$AFFECTED" "$DEPLOYABLE" | jq -s '.[0] as $a | .[1] | map(select(. as $p | $a | index($p)))')
          echo "Code-affected deployable: $ALREADY_DEPLOY"

          # Deployable projects NOT in the affected list — check for infra drift
          UNCHECKED=$(echo "$DEPLOYABLE" "$ALREADY_DEPLOY" | jq -s '.[0] as $d | .[1] as $a | $d | map(select(. as $p | $a | index($p) | not))')
          echo "Checking infra drift for: $UNCHECKED"

          # Compile dependencies so check-infra can evaluate Pulumi programs
          UNCHECKED_CSV=$(echo "$UNCHECKED" | jq -r 'join(",")')
          if [ -n "$UNCHECKED_CSV" ]; then
            pnpm nx run-many --target=compile --projects="$UNCHECKED_CSV" 2>&1 || true
          fi

          # Run check-infra for each unchecked project, detect drift from pulumi output
          DRIFTED='[]'
          for project in $(echo "$UNCHECKED" | jq -r '.[]'); do
            echo "::group::check-infra $project"
            INFRA_OUTPUT=$(pnpm nx run "$project:check-infra" 2>&1) || true
            echo "$INFRA_OUTPUT"
            if echo "$INFRA_OUTPUT" | grep -qE 'to (update|create|delete|replace)'; then
              echo "Drift detected in: $project"
              DRIFTED=$(echo "$DRIFTED" | jq --arg p "$project" '. + [$p]')
            else
              echo "No drift in: $project"
            fi
            echo "::endgroup::"
          done
          echo "Drifted projects: $DRIFTED"

          # Final deploy list: code-affected + infra-drifted
          PROJECTS=$(echo "$ALREADY_DEPLOY" "$DRIFTED" | jq -s '.[0] + .[1] | unique')
          NON_PLATFORM=$(echo "$PROJECTS" | jq -c '[.[] | select(. != "platform")]')

          echo "Final deploy list: $PROJECTS"
          echo "Non-platform: $NON_PLATFORM"

          # web-embed depends on hutch:deploy-infra (Nx dependency), so it must
          # deploy AFTER hutch, not in parallel. Exclude it from the parallel matrix
          # and give it a separate sequential job.
          PARALLEL=$(echo "$NON_PLATFORM" | jq -c '[.[] | select(. != "web-embed")]')

          # Set outputs for downstream jobs to use in their `if:` conditions
          echo "platform-affected=$(echo "$PROJECTS" | jq -e 'index("platform")' > /dev/null 2>&1 && echo true || echo false)" >> "$GITHUB_OUTPUT"
          echo "non-platform-projects=$NON_PLATFORM" >> "$GITHUB_OUTPUT"
          echo "has-non-platform=$(echo "$NON_PLATFORM" | jq -e 'length > 0' > /dev/null 2>&1 && echo true || echo false)" >> "$GITHUB_OUTPUT"
          echo "parallel-projects=$PARALLEL" >> "$GITHUB_OUTPUT"
          echo "has-parallel=$(echo "$PARALLEL" | jq -e 'length > 0' > /dev/null 2>&1 && echo true || echo false)" >> "$GITHUB_OUTPUT"
          echo "web-embed-affected=$(echo "$NON_PLATFORM" | jq -e 'index("web-embed")' > /dev/null 2>&1 && echo true || echo false)" >> "$GITHUB_OUTPUT"
          echo "ff-affected=$(echo "$PROJECTS" | jq -e 'index("firefox-extension")' > /dev/null 2>&1 && echo true || echo false)" >> "$GITHUB_OUTPUT"
          echo "chrome-affected=$(echo "$PROJECTS" | jq -e 'index("chrome-extension")' > /dev/null 2>&1 && echo true || echo false)" >> "$GITHUB_OUTPUT"

  # Platform (EventBridge bus, shared infra) must deploy before any other project
  # because other projects depend on platform resources (e.g. event bus ARN)
  deploy-platform:
    if: needs.detect-projects.outputs.platform-affected == 'true'
    needs: [all-tests, detect-projects]
    uses: ./.github/workflows/project-deployment.yaml
    secrets: inherit
    with:
      project: platform

  # Deploy non-platform projects in parallel (excluding web-embed, which has
  # an Nx dependency on hutch:deploy-infra and must run after hutch finishes)
  pipeline:
    name: ${{ matrix.project }}
    if: >-
      always()
      && needs.all-tests.result == 'success'
      && needs.detect-projects.result == 'success'
      && needs.deploy-platform.result != 'failure'
      && needs.detect-projects.outputs.has-parallel == 'true'
    needs: [all-tests, detect-projects, deploy-platform]
    strategy:
      fail-fast: false
      matrix:
        project: ${{ fromJSON(needs.detect-projects.outputs.parallel-projects) }}
    uses: ./.github/workflows/project-deployment.yaml
    secrets: inherit
    with:
      project: ${{ matrix.project }}

  # web-embed depends on hutch:deploy-infra (Nx dependency graph). Running it
  # in the parallel matrix causes two concurrent `pulumi up` on the hutch stack
  # — one from hutch's own job and one from Nx resolving web-embed's dependency.
  # Deploying it after the pipeline avoids the Pulumi lock conflict.
  deploy-web-embed:
    name: web-embed
    if: >-
      always()
      && needs.all-tests.result == 'success'
      && needs.detect-projects.result == 'success'
      && needs.deploy-platform.result != 'failure'
      && needs.pipeline.result != 'failure'
      && needs.detect-projects.outputs.web-embed-affected == 'true'
    needs: [all-tests, detect-projects, deploy-platform, pipeline]
    uses: ./.github/workflows/project-deployment.yaml
    secrets: inherit
    with:
      project: web-embed

  # Submit Firefox extension to AMO for signing (runs after pipeline deploys infra)
  ff-extension-publish:
    if: needs.detect-projects.outputs.ff-affected == 'true'
    needs: [detect-projects, pipeline, deploy-web-embed]
    permissions:
      contents: write
    uses: ./.github/workflows/submit-ff-extension-for-signing.yml
    secrets: inherit

  # Publish Chrome extension to Chrome Web Store
  chrome-extension-publish:
    if: needs.detect-projects.outputs.chrome-affected == 'true'
    needs: [detect-projects, pipeline, deploy-web-embed]
    permissions:
      contents: write
    uses: ./.github/workflows/publish-chrome-extension.yml
    secrets: inherit
