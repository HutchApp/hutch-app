{
  "name": "@packages/hutch-test-app",
  "targets": {
    "compile": {
      "dependsOn": [
        { "projects": ["hutch"], "target": "compile" }
      ]
    },
    "check": {
      "command": "pnpm lint",
      "options": { "cwd": "{projectRoot}" },
      "dependsOn": [
        { "projects": ["hutch"], "target": "compile" }
      ]
    }
  }
}
