{
  "name": "firefox-extension",
  "targets": {
    "compile": {
      "outputs": ["{projectRoot}/dist-extension", "{projectRoot}/dist-artifacts"]
    },
    "check": {
      "dependsOn": ["^compile"]
    }
  }
}
