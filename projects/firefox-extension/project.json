{
  "name": "firefox-extension",
  "targets": {
    "compile": {
      "outputs": ["{projectRoot}/dist-extension-compiled", "{projectRoot}/dist-extension-files"]
    },
    "check": {
      "dependsOn": ["^compile"]
    }
  }
}
