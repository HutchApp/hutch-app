{
  "name": "firefox-extension",
  "version": "1.0.0",
  "description": "Hutch — Firefox browser extension for saving URLs to a reading list",
  "private": true,
  "scripts": {
    "compile": "tsc",
    "lint": "concurrently --kill-others-on-fail \"tsc --project tsconfig.lint.json\" \"biome lint src\"",
    "test": "jest --testMatch='**/dist/**/*.test.js' --testTimeout=10000",
    "check": "pnpm lint && pnpm compile && pnpm test",
    "dist:extension": "node scripts/build-extension.js"
  },
  "devDependencies": {
    "@types/jest": "29.5.14",
    "concurrently": "9.2.1",
    "esbuild": "0.25.4",
    "jest": "29.7.0",
    "typescript": "5.9.3"
  }
}
