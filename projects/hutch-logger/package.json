{
  "name": "hutch-logger",
  "version": "1.0.0",
  "description": "Shared logging abstraction for Hutch projects",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "scripts": {
    "compile": "tsc",
    "lint": "concurrently --kill-others-on-fail \"tsc --project tsconfig.lint.json\" \"biome lint src\"",
    "test": "echo 'No tests yet'",
    "check": "pnpm compile && pnpm lint"
  },
  "devDependencies": {
    "concurrently": "9.2.1",
    "typescript": "5.9.3"
  }
}
