{
  "name": "hutch",
  "targets": {
    "compile": {
      "dependsOn": [
        { "projects": ["@packages/hutch-logger"], "target": "compile" },
        { "projects": ["@packages/hutch-event-bridge"], "target": "compile" },
        { "projects": ["browser-extension-core"], "target": "compile" }
      ]
    },
    "check": {
      "command": "pnpm lint && pnpm test-with-coverage && pnpm check-infra",
      "options": { "cwd": "{projectRoot}" }
    },
    "test": {},
    "test-integration": {}
  }
}
